\documentclass[12pt]{article}

% --------------------------------------------------
% Preamble
% --------------------------------------------------
% Собирается lualatex'ом (не pdflatex): fontspec + реальный Unicode-шрифт
% вместо T2A/pdflatex - иначе pdflatex перекодирует кириллицу через
% виртуальные (lh) PK-шрифты без корректной ToUnicode-таблицы, и текст,
% скопированный из PDF, превращается в крокозябры (сам PDF при этом
% выглядит нормально - ломается только copy-paste).
\usepackage{fontspec}
\setmainfont{Liberation Serif}
\setsansfont{Liberation Sans}
\setmonofont{Liberation Mono}
\usepackage[russian]{babel}
\usepackage[margin=2.5cm]{geometry}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{float}
\usepackage{hyperref}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  urlcolor=blue!50!black,
  citecolor=blue!50!black
}

\title{Вложение нескольких конечных автоматов в одно состояние:\\
доработка алгоритма умножения на примере счётчика джекпота}
\author{}
\date{\today}

% Диаграммы состояний рендерятся напрямую из исходников PlantUML
% (puml/*.puml - буквальный вывод Automaton.toUML()) во время компиляции.
% Требуется: команда `plantuml` (и Java) в PATH, и компиляция с флагом
% -shell-escape (lualatex -shell-escape article.tex), иначе рендер ниже
% откажет молча и .png не появятся. Под lualatex `\write18` не запускает
% системную команду (в отличие от pdflatex) - нужен `os.execute` через
% `\directlua`.
\begin{document}
\directlua{os.execute("plantuml -tpng puml/a0.puml puml/a1.puml puml/a2.puml puml/a3.puml puml/a4.puml puml/final.puml")}
\maketitle

\noindent
\textbf{Цель работы:} улучшить существующий алгоритм умножения конечных
автоматов так, чтобы он корректно поддерживал вложение нескольких
независимых автоматов в одно и то же состояние родителя, а также вложение
ещё одного автомата в другое состояние родителя, и продемонстрировать это
на реальной модели: добавить в систему «игровой автомат» новый автомат
«Джекпот-счётчик», вложенный наравне с уже существующим автоматом
«Игровые барабаны» в состояние «Игра», и автомат «Звуки», вложенный в
другое состояние того же родителя.

До этой работы алгоритм уже умел умножать пару автоматов и раскрывать один
уровень вложенности (например, автомат «Игровые барабаны» ($A_2$),
вложенный в состояние «Игра» автомата $A_0$). Задача требует другого
случая -- двух вложенных
автоматов в одно и то же состояние одновременно, где один из них должен
опираться на внутреннее состояние другого. Этот случай потребовал
модернизировать сам алгоритм умножения. Теперь в процессе умножения
автоматы помнят «родные» состояния каждого вложенного автомата даже после
слияния в общий автомат. Раньше проверка «автомат сейчас в таком-то
состоянии» работала только пока автоматы ещё не объединили: как только
несколько автоматов сливались в один большой, старые номера состояний
терялись, и проверка переставала быть верной.

\section{Описание подхода}

\subsection*{Этапы алгоритма умножения}

Алгоритм умножения пары автоматов состоит из следующих этапов; их состав не
менялся, требования к вложенности потребовали изменить только то, как на
четвёртом и пятом этапах проверяется состояние вложенного автомата:

\begin{enumerate}
  \item Выбор пары автоматов для перемножения.
  \item Инициализация обхода: начальные состояния пары образуют начальное
        состояние произведения.
  \item Итеративное построение множества вершин: для текущей пары состояний
        перебираются все исходящие переходы обоих автоматов, проверяются
        гварды\footnote{Гвард (guard) -- условие на переходе автомата,
        разрешающее его срабатывание только при выполнении этого условия
        (например, только если некоторый другой автомат сейчас находится в
        определённом состоянии).}, и по совпадающим/относящимся друг к другу
        событиям строятся новые состояния-произведения.
  \item Формирование автомата-произведения из построенного множества вершин
        и рёбер.
  \item Валидация результата -- автомат-произведение прогоняется рядом с
        исходной парой на одних и тех же событиях, и на каждом шаге
        сравниваются их выходы; любое расхождение означает ошибку умножения.
  \item Последовательное перемножение -- для цепочки более чем из двух
        автоматов результат каждого шага становится входом для следующего.
\end{enumerate}

\subsection*{Изменения}

\begin{itemize}
  \item Каждое состояние произведения хранит отображение
        \texttt{native}: автомат $\to$ его «родное» состояние в этом
        состоянии произведения. При слиянии двух состояний в одно эти
        отображения объединяются, поэтому информация не теряется при
        последовательном перемножении цепочки автоматов.
  \item Проверка гварда «автомат $B$ находится в состоянии $k$» (этапы 3--4)
        теперь смотрит в \texttt{native}$[B]$, а не в номер текущего
        (уже составного, чужого) состояния -- именно это раньше
        переставало работать после первого же слияния.
\end{itemize}

Полный код: \href{https://github.com/PhoenixNazarov/fsm-multiply}{\texttt{github.com/PhoenixNazarov/fsm-multiply}}
(\texttt{Main.kt}, \texttt{Multiplier.kt}).

\section{Дополненная система конечных автоматов}

Система «игровой автомат» дополнена двумя новыми автоматами: «Джекпот-счётчик»
($A_3$, вложен вместе с автоматом «Игровые барабаны» в состояние «Игра») и
«Звуки» ($A_4$, вложен независимо в другое состояние того же родителя --
«Выдача жетона»). Ниже приведены диаграмма состояний и таблицы входных и
выходных событий для каждого из пяти автоматов, а также для итогового
перемноженного автомата. На диаграммах метки переходов упрощены до события
и определяющего гварда; полный список входных и выходных событий каждого
автомата приведён в соответствующих таблицах.

\subsection*{Автомат 1. «Игровая машина» ($A_0$)}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{puml/a0.png}
\caption{Диаграмма состояний автомата $A_0$.}
\end{figure}

\input{table_a0.tex}

\clearpage
\subsection*{Автомат 2. «Монетоприемник» ($A_1$)}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{puml/a1.png}
\caption{Диаграмма состояний автомата $A_1$.}
\end{figure}

\input{table_a1.tex}

\clearpage
\subsection*{Автомат 3. «Игровые барабаны» ($A_2$)}

\begin{figure}[H]
\centering
\includegraphics[width=6cm]{puml/a2.png}
\caption{Диаграмма состояний автомата $A_2$.}
\end{figure}

\input{table_a2.tex}

\clearpage
\subsection*{Автомат 4. «Джекпот-счётчик» ($A_3$)}

\begin{figure}[H]
\centering
\includegraphics[width=6cm]{puml/a3.png}
\caption{Диаграмма состояний автомата $A_3$ -- нового вложенного автомата.}
\end{figure}

\input{table_a3.tex}

Обратите внимание: входы $A_3$ включают опрос состояния соседа
\texttt{y2=3} автомата $A_2$ -- то самое обращение к внутреннему
состоянию второго вложенного автомата, ради которого дорабатывался
алгоритм.

\clearpage
\subsection*{Автомат 5. «Звуки» ($A_4$)}

\begin{figure}[H]
\centering
\includegraphics[width=6.5cm]{puml/a4.png}
\caption{Диаграмма состояний автомата $A_4$ -- независимо вложенного
  автомата.}
\end{figure}

\input{table_a4.tex}

$A_4$ вложен не в «Игра», а в другое состояние родителя -- «Выдача
жетона». У $A_4$ одно состояние и один безусловный переход по тому же
\texttt{e06}, который родитель и так обрабатывает в «Выдача жетона» --
новых условий это не добавляет, поэтому противоречивых переходов не
возникает.

\clearpage
\section{Итоговый перемноженный автомат}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{puml/final.png}
\caption{Диаграмма состояний итогового автомата
  $A_0\times A_1\times A_2\times A_3\times A_4$ (9 состояний). Название
  каждого составного состояния выбрано авторами так, чтобы диаграмму было
  легче читать; полное соответствие номера состояния всем пяти исходным
  состояниям приведено в примечании ниже.}
\end{figure}

\footnotesize
\input{legend_final.tex}
\normalsize

\input{table_final.tex}

\clearpage
\section*{Вывод}

Оба новых вложенных автомата вместе добавили всего одно новое состояние
(с 8 до 9), а не увеличили итоговый автомат в несколько раз, при этом
$A_4$ -- вложенный отдельно, в другое состояние -- вообще не увеличил
число состояний: 9 состояний осталось после его добавления таким же, как
и до него. Это позволило всё так же понимать, что происходит в итоговом
перемноженном автомате.

\end{document}
